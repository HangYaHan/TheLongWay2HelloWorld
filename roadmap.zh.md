# 项目路线图
## 通往 Hello 的漫漫长路

**目标：** 在 Zynq/FPGA 上构建或集成一个极简 CPU/软核，让它打印出 Hello World，然后向中断、RTOS 和系统级理解逐步扩展。

---

## 阶段 0 —— 明确使命

**目标：**

确定什么算"完成"，并选择一条可行的路线。

**需要做的决定：**

- **平台：**
  - Zynq-7000 开发板或其他 FPGA 开发板
- **CPU 路线：**
  - 路线 A：先使用现成的软核（MicroBlaze / PicoRV32 / VexRiscv）
  - 路线 B：先构建你自己的简化 CPU
  - 推荐：先使用现成软核，再构建你自己的简化 CPU
- **最终里程碑：**
  - 最低：CPU + 内存 + UART + 裸机程序 + Hello World
  - 更好：再加上定时器中断 + GPIO + 简单 monitor
  - 进阶：再加上 FreeRTOS 或一个小型调度器
  - 扩展：在 Zynq 的 ARM 侧运行 Linux，或使用支持操作系统的更强软核

**交付物：**

- 一页纸的项目定义
- 选定的开发板、工具链、仓库结构
- 最终项目名称

**验收标准：**

- 你能用 3 句话清楚说明你的最终目标
- 你清楚自己要首先走哪条路线

**风险：**

- 过早选定过于宏大的最终目标
- 路线切换过于频繁

---

## 阶段 1 —— 数字设计与工具链基础

**目标：**

获得足够的数字设计与 FPGA 工作流知识，从而能够构建并调试简单的硬件模块。

**主题：**

- 基础数字逻辑
  - 组合逻辑
  - 时序逻辑
  - 触发器
  - 有限状态机
- Verilog/SystemVerilog 基础
  - 模块
  - always 块
  - 阻塞赋值与非阻塞赋值
  - 参数化
- FPGA 工作流
  - 仿真
  - 综合
  - 实现
  - 比特流生成
- 调试方法
  - 波形阅读
  - 测试平台（testbench）设计
  - 板级调试基础

**小项目：**

- LED 闪烁
- UART 发送器
- UART 接收器
- 定时器/计数器
- 简单 GPIO 外设

**交付物：**

- UART TX/RX 的 Verilog 源码
- UART 和定时器的测试平台
- 能让 LED 闪烁并通过 UART 发送一个字节的 FPGA 工程

**验收标准：**

- 你能解释 UART 模块中的每一个信号
- 你能用波形调试，而不是靠猜
- 你能综合并在板子上运行一个小设计

**风险：**

- 跳过仿真
- 把 FPGA 当软件对待，不尊重时序与顺序

---

## 阶段 2 —— 计算机组成基础

**目标：**

理解 CPU 如何执行指令，以及软件如何映射到硬件。

**主题：**

- ISA 基础
  - 寄存器
  - 算术指令
  - load/store
  - 分支/跳转
- 数据通路与控制
  - ALU
  - 寄存器堆
  - 程序计数器
  - 指令存储器
  - 数据存储器
- CPU 风格
  - 单周期
  - 多周期
  - 流水线基础
- 异常与中断
- 内存映射 I/O

**练习：**

- 手工执行简单的汇编程序
- 画出 add/load/store/branch 的数据通路
- 解释一个 C 循环如何变成指令

**交付物：**

- 你选定的 ISA 子集的笔记
- 你的极简 CPU 数据通路图
- 指令执行过程的逐步讲解

**验收标准：**

- 你能准确解释取指/译码/执行期间发生了什么
- 你能把一个简单的 C 程序映射到汇编并映射到数据通路行为

**风险：**

- 死记硬背教材图，却不理解信号流动
- 在理解执行模型之前就一头扎进 RTL

---

## 阶段 3 —— 用现成软核构建极简系统

**目标：**

通过让一个已知可用的 CPU 在 FPGA/Zynq 上打印 Hello World 来建立信心。

**推荐选项：**

- 若采用 Xilinx 优先的流程，用 MicroBlaze
- 若想要透明和简单，用 PicoRV32
- 若想要更强的 RISC-V 生态，用 VexRiscv

**系统组件：**

- CPU 核
- BRAM 或片上内存
- UART
- 定时器
- 可选 GPIO
- 简单互连或内存映射

**主题：**

- 地址映射
- ROM 与 RAM 的布局
- MMIO 读写
- 启动代码
- 链接脚本
- 交叉编译

**任务：**

- 集成 CPU 与内存
- 集成 UART 外设
- 编写启动代码
- 编写链接脚本
- 构建裸机 C 程序
- 让 UART 打印 Hello World

**交付物：**

- 极简 SoC 框图
- 内存映射文档
- startup.S
- linker.ld
- hello.c
- FPGA 比特流 / 工程文件

**验收标准：**

- 复位后，CPU 从预期地址开始执行
- UART 输出确定的字符
- 上电或复位后 Hello World 可靠出现

**风险：**

- 复位向量错误
- 链接地址错误
- 栈未初始化
- UART 波特率时序不匹配
- 因内存集成错误导致 CPU 取到垃圾数据

---

## 阶段 4 —— 理解从 C 到 Hello 的软件路径

**目标：**

理解让 Hello World 显示出来所涉及的每一层。

**主题：**

- 编译器输出
  - 从 C 生成汇编
  - 检查目标文件
- 链接
  - 段（section）
  - 符号放置
  - 内存区域
- 启动
  - 初始化栈指针
  - 需要时复制 data 段
  - 清零 bss
  - 跳转到 main
- UART 驱动
  - 轮询式发送
- 二进制加载格式
  - ELF 基础
  - 二进制镜像生成

**任务：**

- 把 hello.c 编译成汇编
- 阅读生成的汇编
- 在 ELF 中跟踪符号
- 验证内存镜像被加载进 FPGA 内存
- 确认到 UART 的 MMIO 写路径

**交付物：**

- Hello World 的带注释汇编
- ELF 段映射
- 文档：《一个字符如何到达 UART》

**验收标准：**

- 你能解释这条精确链条：
  C 源码 -> 汇编 -> 机器码 -> 内存镜像 -> CPU 执行 -> UART 发送 -> 串口终端
- 你能在你的构建产物中定位 main、栈、bss 和 UART 基地址

**风险：**

- 把工具链当成魔法
- 不理解 ELF 与实际 FPGA 内存镜像之间的关系

---

## 阶段 5 —— 构建你自己的简化 CPU

**目标：**

创建一个小型 CPU，能执行一个极简程序，并最终打印 Hello World。

**推荐策略：**

从一个很小的 ISA 子集开始。
不要一开始就做流水线、缓存、MMU 或复杂的特权支持。

**建议的指令子集：**

- 算术/逻辑
  - add
  - sub
  - and
  - or
  - xor
- 立即数操作
- Load/store
- 分支/跳转
- 可选移位
- 可选比较/置位

**微架构：**

- 先做单周期，或一个非常简单的多周期核

**硬件模块：**

- PC
- 指令存储器接口
- 译码器
- 寄存器堆
- ALU
- 数据存储器接口
- 分支逻辑
- 控制单元

**开发顺序：**

1. ALU
2. 寄存器堆
3. PC 更新逻辑
4. 指令译码
5. 执行算术指令
6. 加入 load/store
7. 加入分支/跳转
8. 运行手写汇编程序
9. 连接 MMIO UART
10. 打印单个字符
11. 打印 Hello World

**交付物：**

- CPU RTL
- 测试平台套件
- ISA 子集文档
- 示例程序的执行轨迹

**验收标准：**

- CPU 通过每条指令的定向测试
- CPU 能正确运行一个小循环
- CPU 能向 UART MMIO 写入一个字节
- Hello World 由你自己的 CPU 打印出来

**风险：**

- 把 ISA 做得太大
- 在验证之前就实现过多指令
- 没有正式或系统的测试策略

---

## 阶段 6 —— 中断、定时器与真实嵌入式行为

**目标：**

从"一个能跑代码的 CPU"迈向"一个能对事件做出反应的系统"。

**主题：**

- 定时器外设
- 中断请求与应答
- 中断向量或 trap 入口
- 上下文保存/恢复
- 从中断返回
- 轮询式 I/O 与中断驱动式 I/O

**任务：**

- 加入定时器中断
- 用汇编/C 编写中断处理程序
- 在周期性中断时闪烁或打印
- 如果可用，加入外部中断源
- 如有需要，构建一个基本的中断控制器

**交付物：**

- 定时器模块
- 中断入口/退出代码
- 使用定时器中断的示例程序

**验收标准：**

- 周期性中断以预期间隔发生
- 中断后主程序正确恢复
- 跨中断时寄存器不被破坏

**风险：**

- 上下文保存不完整
- 中断使能/禁止逻辑错误
- 可重入问题

---

## 阶段 7 —— RTOS 概念与微型调度器

**目标：**

把硬件陷阱/定时器与操作系统机制连接起来。

**主题：**

- 任务控制块
- 栈布局
- 上下文切换
- Tick 中断
- 协作式与抢占式调度
- 临界区
- 队列/信号量基础

**策略：**

- 方案 A：自己写一个微型协作式调度器
- 方案 B：移植一个小型 RTOS，例如 FreeRTOS
- 推荐：先写一个小型调度器，再研究或移植 FreeRTOS

**任务：**

- 创建两个拥有独立栈的任务
- 实现手动上下文切换
- 由定时器中断触发切换
- 加入简单的轮转调度
- 加入一个 UART 输出任务和一个闪烁任务

**交付物：**

- 微型调度器源码
- 任务栈初始化例程
- 多任务演示

**验收标准：**

- 多个任务正确运行
- 上下文切换保留程序状态
- 定时器驱动调度事件

**风险：**

- 栈帧格式错误
- 保存的寄存器过少或过多
- 在可观测性不足的情况下调试上下文切换

---

## 阶段 8 —— Zynq ARM 侧与 Linux 系统视角

**目标：**

如果使用 Zynq，学习一个生产级硬处理器系统如何运行 Linux，并如何与 PL 硬件通信。

**为什么这个阶段重要：**
即使你自己的软核永远不运行 Linux，理解 Zynq 上的 Linux 也能帮你补上缺失的系统级全貌。

**主题：**

- Zynq PS/PL 架构
- AXI 基础
- U-Boot
- Linux 内核引导
- 设备树
- PL 中的内存映射外设
- 简单内核驱动或用户态 mmap 访问

**任务：**

- 构建一个简单的 PL 外设
- 通过 AXI 将其暴露给 ARM/Linux
- 从 Linux 用户态访问它
- 可选：编写一个基本的内核模块/驱动

**交付物：**

- PL 外设
- 设备树修改
- Linux 侧测试程序或驱动

**验收标准：**

- ARM 上的 Linux 能与 PL 外设通信
- 你能解释从硬件寄存器到用户态读/写的路径

**风险：**

- 迷失在 Linux 构建的复杂性中
- 在发行版/构建系统问题上花太多时间，而不是学习核心概念

---

## 阶段 9 —— 打磨、文档与作品集

**目标：**

把你的旅程转化为一件专业的作品集成果。

**要记录的内容：**

- 系统架构
- ISA 子集
- CPU 微架构
- 内存映射
- 构建流程
- 启动流程
- 调试方法
- 经验教训
- 后续步骤

**要准备的成果物：**

- 结构清晰的 Git 仓库
- 带架构图的 README
- 演示视频：
  - 开发板复位
  - 串口终端显示 Hello World
  - 可选的中断/RTOS 演示
- 技术文章：《从门电路到 Hello》
- 简短的简历要点

**简历要点示例：**

- 在 FPGA 上设计并实现了一个带 UART、定时器和内存映射 I/O 的极简 CPU/SoC
- 构建了裸机软件流程，包括启动代码、链接脚本和交叉编译
- 在自定义/极简硬件上成功跑通了 Hello World 应用
- 实现了中断处理，并探索了任务切换 / RTOS 概念

**验收标准：**

- 另一位工程师能根据你的仓库复现该项目
- 你的 README 讲述了一个从架构到演示的连贯故事
- 你的简历要点具体可量化

**风险：**

- 完成了技术部分，却没有很好地包装
- 低估了良好文档的价值

---

## 推荐里程碑阶梯

- **里程碑 1：** FPGA 上 LED 闪烁
- **里程碑 2：** UART 发送单个字节
- **里程碑 3：** 极简软核系统运行起来
- **里程碑 4：** 在现成软核上跑通裸机 Hello World
- **里程碑 5：** 你自己的简化 CPU 执行算术测试程序
- **里程碑 6：** 你自己的 CPU 向 UART 写入一个字节
- **里程碑 7：** 你自己的 CPU 打印 Hello World
- **里程碑 8：** 定时器中断工作
- **里程碑 9：** 基本调度器或 RTOS 演示工作
- **里程碑 10：** Zynq Linux 侧与 PL 外设通信

---

## 成功的样子

**最低成功：**

- 你理解从 C 源码到 UART 输出的完整路径
- 你能构建并调试一个基于极简 CPU 的系统
- 你在 FPGA 上有一个可工作的 Hello World

**强力成功：**

- 你构建了自己的简化 CPU，并让它打印出 Hello World
- 你实现了中断和基本驱动
- 你能解释启动、链接、MMIO 和任务切换

**卓越成功：**

- 你把这项工作与 RTOS 或 Linux 连接起来
- 你把它呈现为一个打磨过的系统级项目作品集
- 你已为底层系统方向的面试做好准备

---

## 建议的书籍学习顺序

1. 《Digital Design and Computer Architecture》
2. 《Computer Organization and Design》（若可能，选 RISC-V 版）
3. 《Operating Systems: Three Easy Pieces》
4. 《Computer Systems: A Programmer's Perspective》
5. 按需查阅厂商文档 / ISA 规范 / 软核项目文档

---

## 整段旅程的心智模型

门电路 -> 寄存器 -> 数据通路 -> CPU -> 总线 -> 外设 -> 内存映射 -> 启动 -> C 运行时 -> 驱动 -> 中断 -> 调度器 -> 操作系统

如果你能走通这条链条并解释每一次转换，你就真正建立了理解。
